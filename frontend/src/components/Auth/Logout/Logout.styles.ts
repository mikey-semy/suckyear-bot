import styled from 'styled-components';
import { 
    BaseIconButtonContainer, 
    BaseIconButtonTitle, 
    BaseIconButtonIcon 
  } from '@/components/Common/Button/IconButton.styles';

export const LogoutButtonContainer = styled(BaseIconButtonContainer)``;
export const LogoutButtonTitle = styled(BaseIconButtonTitle)``;
export const LogoutButtonIcon = styled(BaseIconButtonIcon)``;
