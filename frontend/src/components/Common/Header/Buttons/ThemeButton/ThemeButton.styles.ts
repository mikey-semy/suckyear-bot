import styled from 'styled-components';
import { 
    BaseIconButtonContainer, 
    BaseIconButtonTitle, 
    BaseIconButtonIcon 
  } from '@/components/Common/Button/IconButton.styles';
export const ThemeButtonContainer = styled(BaseIconButtonContainer)``;
export const ThemeButtonTitle = styled(BaseIconButtonTitle)``;
export const ThemeButtonIcon = styled(BaseIconButtonIcon)``;
